const NavBar = () => {
    return (
        <nav>
            <a href="/">Home</a>
            <a href="/login">Login</a>
            <a href="/user-list">User List</a>
        </nav>
    );
}

export default NavBar;