const NavBar = () => {
    return (
        <nav>
            <a href="/">Home</a>
            <a href="/login">Login</a>
            <a href="/crud-post">CRUD App</a>
        </nav>
    );
}

export default NavBar;